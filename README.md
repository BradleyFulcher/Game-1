# Squirrel Side Scroller

This is a simple side scroller game built with Python's built-in `tkinter` module.
A squirrel collects acorns and jumps over rocks. Press `Space` to jump.

Run the game with:

```bash
python3 squirrel_game.py
```

Collect as many acorns as you can without hitting any rocks!
